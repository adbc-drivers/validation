# TxtCase VSCode Extension

Barebones VSCode extension for TxtCase.

## Installation

For testing, you can install the extension by copying it into your user-level VSCode extensions folder,

```sh
cp -r ../txtcase ~/.vscode/extensions/adbc-drivers.txtcase-syntax-0.0.1
```
